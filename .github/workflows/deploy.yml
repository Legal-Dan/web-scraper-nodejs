name: deploy

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run app
        run: npm install && SQL_PASSWORD=${{ secrets.SQL_PASSWORD }} npm run start
